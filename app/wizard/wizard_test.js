'use strict';

describe('myApp.wizard module', function() {

  beforeEach(module('myApp.wizard'));

  describe('wizard controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var wizardCtrl = $controller('WizardCtrl');
      expect(wizardCtrl).toBeDefined();
    }));

  });
});
